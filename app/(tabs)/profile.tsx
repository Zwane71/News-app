import { SafeAreaView, ScrollView, View, Text } from "react-native";

export default function Profile() {
	return (
		<SafeAreaView>
			<ScrollView>
				<View>
					<Text>Profile</Text>
				</View>
			</ScrollView>
		</SafeAreaView>
	);
}
