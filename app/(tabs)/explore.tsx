import { SafeAreaView, ScrollView, View, Text } from "react-native";

export default function Explore() {
	return (
		<SafeAreaView>
			<ScrollView>
				<View>
					<Text>Explore</Text>
				</View>
			</ScrollView>
		</SafeAreaView>
	);
}
