import { SafeAreaView, ScrollView, View, Text } from "react-native";

export default function Bookmark() {
	return (
		<SafeAreaView>
			<ScrollView>
				<View>
					<Text>Bookmark</Text>
				</View>
			</ScrollView>
		</SafeAreaView>
	);
}
